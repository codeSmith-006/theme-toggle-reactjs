export default {
  // For v4 many things are auto-detected, but you can still customize theme, plugins, etc.
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  // You can omit darkMode here when relying on CSS @variant, but you can also keep it:
  // darkMode: 'class',
  theme: {
    extend: {},
  },
  plugins: [],
};
